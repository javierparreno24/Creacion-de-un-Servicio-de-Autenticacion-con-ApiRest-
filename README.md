🚀 Proyecto: Servicio de Autenticación con API RESTful y JWT (Simulado)Este proyecto corresponde a la Actividad Específica Evaluable (AEE) del Resultado de Aprendizaje 4 (RA4) para el módulo profesional DWEC (Desarrollo Web en Entorno Cliente)111.El objetivo es crear una aplicación web básica que implemente un sistema de autenticación de usuarios mediante una API RESTful utilizando PHP y HTML2. El sistema simula el uso de un JSON Web Token (JWT) 3, utilizando una cadena codificada con base64_encode como token4.🎯 Características de la AplicaciónCaracterísticaDescripciónRequisito TécnicoPantalla de LoginFormulario HTML para introducir Nombre de usuario y Contraseña5.Utiliza JavaScript y fetch para enviar las credenciales al endpoint6.API RESTful: AutenticaciónEndpoint POST /api/login7777.Valida credenciales contra un array predefinido en PHP8. Devuelve un token si es correcto 9, o 401 Unauthorized si es incorrecto10.API RESTful: BienvenidaEndpoint protegido GET /api/welcome111111.Requiere el token de autenticación en la cabecera (Authorization: Bearer <token>)121212. Responde con datos del usuario si el token es válido13.Pantalla de BienvenidaMuestra mensaje personalizado con el nombre de usuario autenticado y la hora actual14.Obtiene los datos del usuario llamando al endpoint protegido /api/welcome15.Manejo de PermisosSi se accede sin un token válido, la API responde con código HTTP 403 Forbidden16.El cliente redirige automáticamente a una página de "No tienes permisos"171717.Cerrar SesiónBotón/enlace en la pantalla de bienvenida18.Elimina el token almacenado en el cliente (localStorage)19. Redirige al usuario a la pantalla de login20.🛠️ Requisitos Técnicos ClaveImplementación de API RESTful en PHP21:Uso de rutas simples para manejar las solicitudes: POST /api/login y GET /api/welcome22.Utiliza json_encode para devolver respuestas en formato JSON23.Manejo de Sesiones en el Cliente24:Usar localStorage para almacenar el token devuelto por el servidor25.Enviar el token en las cabeceras HTTP: Authorization: Bearer <token> para solicitudes protegidas26.Validación de Credenciales en el Servidor27:Se debe usar el siguiente array predefinido en PHP para simular la lista de usuarios28:PHP$usuarios = [
    ["username" => "admin", "password" => "1234"],
    ["username" => "user", "password" => "abcd"]
];
Redirecciones en el Cliente29:Si el servidor responde con un código de error (como 401 o 403), redirigir automáticamente a la pantalla correspondiente30.🖥️ Estructura del Proyecto (Sugerida)/
|-- index.html (Pantalla de Login)
|-- welcome.html (Pantalla de Bienvenida)
|-- no-permisos.html (Pantalla de No Tienes Permisos)
|-- api/
|   |-- index.php (Manejaría POST /api/login y GET /api/welcome)
|-- js/
|   |-- auth.js (Lógica de fetch, manejo de token, redirecciones)
